import React from "react";
import "./Header.css";
import MobileMenu from "./MobileMenu";

const Header = () => {
  return (
    <header>
      <MobileMenu />
    </header>
  );
};

export default Header;
